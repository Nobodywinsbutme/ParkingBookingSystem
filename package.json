{
  "name": "smart-parking",
  "version": "0.1.0",
  "private": true,
  "description": "Spring Boot monolith in be/ (JSP + MySQL). Run locally: see docs/chạy.md; use Maven below.",
  "scripts": {
    "build": "mvn -f be/pom.xml package",
    "test": "mvn -f be/pom.xml test",
    "be:compile": "mvn -f be/pom.xml compile",
    "be:run": "mvn -f be/pom.xml spring-boot:run",
    "be:clean": "mvn -f be/pom.xml clean"
  }
}
