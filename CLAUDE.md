@AGENTS.md

Local run (MySQL + Spring Boot): see `docs/chạy.md`. Optional: `npm run be:run` from repo root (`package.json` wraps Maven). SQL/courseware slides: `docs/sql.md` (see `QueryDemoRestController` for JSON demos).
